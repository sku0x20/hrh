// Basic integration test to ensure the tests directory is set up correctly.
// This does not rely on any crate API since src/lib.rs is currently empty.

#[test]
fn it_runs() {
    assert_eq!(2 + 2, 4);
}
